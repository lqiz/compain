# 真机测试配置指南

## 🔧 问题说明

在微信小程序真机测试时，手机无法访问 `localhost`，会出现 `connection_refused` 错误。

## 📱 解决方案

### 步骤 1：获取你的局域网 IP 地址

在终端运行以下命令获取本机 IP：

```bash
hostname -I
# 或者
ip addr show
```

**示例输出**：
```
9.129.72.122 169.254.111.192
```

通常第一个 IP 就是你的局域网 IP（如 `9.129.72.122`）。

### 步骤 2：修改环境变量

编辑 `.env.local` 文件，将 `localhost` 替换为你的局域网 IP：

```bash
# 开发环境配置
# PROJECT_DOMAIN=http://localhost:3000  # ❌ 真机无法访问
PROJECT_DOMAIN=http://9.129.72.122:3000  # ✅ 使用你的局域网IP
```

**注意**：将 `9.129.72.122` 替换为你实际的局域网 IP！

### 步骤 3：确保后端监听 0.0.0.0

后端已配置为监听 `0.0.0.0`（所有网络接口），无需额外修改。

### 步骤 4：重启开发服务

```bash
# 停止当前服务（Ctrl+C）
# 然后重新启动
cd /workspace/projects
coze dev
```

### 步骤 5：配置微信小程序开发者工具

1. 打开微信开发者工具
2. 点击右上角「详情」
3. 选择「本地设置」
4. 确保勾选以下选项：
   - ✅ 不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书
   - ✅ 使用 npm 模块（如需要）

### 步骤 6：真机测试

1. 在微信开发者工具中点击「预览」
2. 使用微信扫描二维码
3. 在真机中测试上传功能

## 🔍 验证步骤

### 验证后端服务

在真机浏览器中访问：
```
http://你的局域网IP:3000/api/video/list
```

应该能看到 JSON 格式的视频列表数据。

### 验证前端配置

在微信开发者工具的 Console 中查看：
```
[Network] URL: http://你的局域网IP:3000/api/video/upload
```

确认 URL 中使用的是局域网 IP 而不是 localhost。

## ⚠️ 常见问题

### 问题 1：仍然无法连接

**可能原因**：
- 手机和电脑不在同一个局域网
- 防火墙阻止了连接
- IP 地址错误

**解决方法**：
1. 确认手机和电脑连接同一个 Wi-Fi
2. 临时关闭防火墙测试
3. 重新检查 IP 地址

### 问题 2：IP 地址变化

**原因**：电脑重启或重新连接 Wi-Fi 后，IP 可能变化

**解决方法**：
1. 重新获取 IP 地址
2. 更新 `.env.local` 文件
3. 重启开发服务

### 问题 3：上传大文件超时

**解决方法**：
- 确保 Wi-Fi 信号良好
- 尝试使用较小的视频文件测试
- 检查网络连接稳定性

## 📋 完整检查清单

- [ ] 获取了正确的局域网 IP 地址
- [ ] 修改了 `.env.local` 中的 `PROJECT_DOMAIN`
- [ ] 后端监听 `0.0.0.0`（已配置）
- [ ] 重启了开发服务
- [ ] 微信开发者工具勾选了「不校验合法域名」
- [ ] 手机和电脑在同一 Wi-Fi
- [ ] 真机可以访问 `http://IP:3000/api/video/list`
- [ ] 测试了视频上传功能

## 🎯 快速命令参考

```bash
# 获取 IP 地址
hostname -I

# 查看当前 IP
ip addr show | grep "inet "

# 检查端口监听
ss -tuln | grep 3000

# 测试后端服务（从另一台设备）
curl http://你的IP:3000/api/video/list
```

## 💡 提示

- 开发阶段使用局域网 IP 测试
- 正式部署时使用公网域名
- 记得在 `.gitignore` 中忽略 `.env.local`（已配置）
- 如果团队协作，可以创建 `.env.example` 作为模板
