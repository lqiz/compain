# 📱 真机小程序问题完全解决方案

## 🔍 问题分析

微信小程序真机无法上传/加载视频，主要原因：

### 1. 微信小程序网络限制
- ❌ **不能直接访问局域网 IP**（`http://192.168.x.x:3000`）
- ❌ **不能直接访问 localhost**
- ✅ **必须使用公网域名 + HTTPS**
- ✅ **域名必须在微信后台配置白名单**

### 2. 视频加载限制
- ❌ **本地视频路径无法直接播放**
- ✅ **必须使用 HTTPS 公网 URL**

---

## 🚀 完整解决方案

### 方案一：使用开发工具预览（推荐用于开发）

#### 步骤 1：在微信开发者工具中测试
```
1. 打开微信开发者工具
2. 导入项目（选择项目根目录）
3. 在开发者工具中测试，不需要公网域名
4. 开发者工具支持访问 localhost:3000
```

#### 步骤 2：配置开发者工具
```
微信开发者工具 → 详情 → 本地设置：
✅ 勾选「不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书」
```

---

### 方案二：使用内网穿透工具（推荐用于真机测试）

#### 使用 ngrok 或 localtunnel

**步骤 1：安装并启动内网穿透**

```bash
# 方式 1：使用 localtunnel（简单，不需要注册）
npx localtunnel --port 3000

# 方式 2：使用 ngrok（更稳定，需要注册）
# 先访问 https://ngrok.com 注册并获取 token
ngrok config add-authtoken <你的token>
ngrok http 3000
```

**步骤 2：获取公网 URL**

启动后会显示类似：
```
Forwarding  https://xyz-123-45-67-89.loca.lt -> http://localhost:3000
```

**步骤 3：更新项目配置**

```bash
# 编辑 .env.local
PROJECT_DOMAIN=https://xyz-123-45-67-89.loca.lt
```

**步骤 4：重启开发服务**

```bash
# 按 Ctrl+C 停止当前服务，然后重新启动
cd /workspace/projects && coze dev
```

---

### 方案三：部署到公网服务器（生产环境）

#### 步骤 1：购买云服务器和域名
- 阿里云/腾讯云/华为云等
- 购买域名（如：`example.com`）

#### 步骤 2：配置 HTTPS 证书
- 使用 Let's Encrypt 免费证书
- 或使用云服务商的免费证书

#### 步骤 3：配置 Nginx 反向代理
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 步骤 4：在微信后台配置域名白名单
```
登录微信公众平台 → 开发 → 开发管理 → 开发设置 → 服务器域名
在「request合法域名」中添加：https://your-domain.com
在「uploadFile合法域名」中添加：https://your-domain.com
在「downloadFile合法域名」中添加：https://your-domain.com
```

---

## 📋 当前快速测试步骤

### 立即测试（使用微信开发者工具）

1. **在当前环境继续开发**
   ```
   后端服务已在运行：http://localhost:3000
   前端服务已在运行：http://localhost:5000
   ```

2. **使用微信开发者工具测试**
   ```bash
   # 1. 下载并安装微信开发者工具
   # https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   
   # 2. 打开微信开发者工具，导入项目
   # 选择项目目录：/workspace/projects
   
   # 3. 在开发者工具中测试（支持 localhost）
   ```

3. **配置开发者工具**
   ```
   详情 → 本地设置：
   ✅ 勾选「不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书」
   ```

---

## 🔧 检查清单

### 检查 1：后端服务是否正常
```bash
curl http://localhost:3000/api/video/list
```
应该返回视频列表数据。

### 检查 2：局域网 IP 是否正确
```bash
node get-ip.js
```
查看你的局域网 IP。

### 检查 3：微信小程序是否在真机上
- 真机上 → 需要公网 HTTPS
- 开发者工具中 → 可以用 localhost

---

## 💡 推荐开发流程

### 阶段 1：开发阶段
```
✅ 使用微信开发者工具
✅ 使用 localhost:3000
✅ 快速迭代开发
```

### 阶段 2：真机测试
```
✅ 使用内网穿透（ngrok/localtunnel）
✅ 获取临时公网 HTTPS URL
✅ 配置 .env.local
✅ 真机测试
```

### 阶段 3：生产发布
```
✅ 购买域名和服务器
✅ 配置 HTTPS 证书
✅ 配置微信后台白名单
✅ 正式发布
```

---

## 🎯 下一步操作

**请告诉我**：
1. 你现在是用真机测试还是用微信开发者工具？
2. 如果是真机，你想用内网穿透方案还是部署到服务器？

我可以根据你的选择提供更详细的操作指导！
