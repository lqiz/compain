# 📱 手机小程序部署方案

## 问题确认

✅ **你说得对！**
- 手机上**肯定不能用 localhost**
- 手机上**也不能用局域网 IP**
- 必须使用 **HTTPS 公网域名**

---

## 🚀 方案一：内网穿透（临时测试，最快）

### 优点
- ✅ 不需要买服务器和域名
- ✅ 5 分钟内就能用
- ✅ 免费

### 缺点
- ❌ URL 是临时的，重启后会变
- ❌ 速度可能较慢
- ❌ 不适合生产环境

### 操作步骤

**步骤 1：启动内网穿透**

```bash
# 在项目根目录执行
cd /workspace/projects

# 方式 A：使用 localtunnel（最简单，无需注册）
npx localtunnel --port 3000

# 方式 B：使用 ngrok（更稳定，需要注册）
# 先访问 https://ngrok.com 注册获取 token
ngrok config add-authtoken <你的token>
ngrok http 3000
```

**步骤 2：获取公网 URL**

启动后会显示类似：
```
Forwarding  https://xyz-123-45-67-89.loca.lt -> http://localhost:3000
```

**步骤 3：更新配置**

```bash
# 编辑 .env.local
PROJECT_DOMAIN=https://xyz-123-45-67-89.loca.lt
```

**步骤 4：重启开发服务**

```bash
# 按 Ctrl+C 停止当前服务，然后重新启动
cd /workspace/projects && coze dev
```

---

## 🏢 方案二：云服务器部署（长期使用，推荐）

### 优点
- ✅ 稳定的公网域名
- ✅ 速度快
- ✅ 适合生产环境

### 缺点
- ❌ 需要买服务器和域名
- ❌ 需要一些配置工作
- ❌ 需要一点成本

### 成本估算

**入门级配置**（足够小程序初期使用）：
- 云服务器：约 50-100 元/月
- 域名：约 50-100 元/年
- **总计**：约 100 元/月

### 操作步骤

#### 步骤 1：购买云服务器

推荐选择：
- **阿里云**：https://www.aliyun.com
- **腾讯云**：https://cloud.tencent.com
- **华为云**：https://www.huaweicloud.com

**配置建议**：
- CPU：2 核
- 内存：4GB
- 带宽：3Mbps
- 系统：Ubuntu 20.04 或 22.04

#### 步骤 2：购买域名

在同一云服务商购买域名，例如：
- `myshare.com`
- `kids-app.com`

#### 步骤 3：配置 DNS 解析

在域名管理中添加解析：
```
类型：A
主机记录：@ 或 api
记录值：你的服务器公网 IP
```

#### 步骤 4：配置 HTTPS 证书

**使用 Let's Encrypt 免费证书**：
```bash
# 在服务器上安装 certbot
sudo apt-get update
sudo apt-get install certbot

# 获取证书
sudo certbot certonly --standalone -d your-domain.com
```

**或使用云服务商免费证书**：
- 阿里云/腾讯云都提供免费 SSL 证书

#### 步骤 5：在服务器上部署项目

**5.1 登录服务器**
```bash
ssh root@your-server-ip
```

**5.2 安装环境**
```bash
# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装 pnpm
npm install -g pnpm

# 安装 nginx
sudo apt-get install -y nginx
```

**5.3 上传项目代码**
```bash
# 在本地打包项目
cd /workspace/projects
pnpm build:server

# 将 dist-server 目录上传到服务器
# 使用 scp 或 git 等方式
```

**5.4 在服务器上运行后端**
```bash
# 在服务器上
cd /path/to/project
pnpm install --production
node dist-server/main.js
```

**5.5 配置 Nginx 反向代理**

编辑 `/etc/nginx/sites-available/default`：
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 支持大文件上传
        client_max_body_size 100M;
    }
}
```

重启 Nginx：
```bash
sudo nginx -t
sudo systemctl restart nginx
```

#### 步骤 6：配置微信小程序后台

登录 [微信公众平台](https://mp.weixin.qq.com)：

```
开发 → 开发管理 → 开发设置 → 服务器域名

在「request合法域名」中添加：
https://your-domain.com

在「uploadFile合法域名」中添加：
https://your-domain.com

在「downloadFile合法域名」中添加：
https://your-domain.com
```

#### 步骤 7：更新项目配置

```bash
# 编辑 .env.local
PROJECT_DOMAIN=https://your-domain.com
```

---

## 🔧 方案三：使用 Serverless 服务（最简单）

### 优点
- ✅ 不需要管理服务器
- ✅ 按使用量付费，成本低
- ✅ 自动扩缩容

### 推荐服务

1. **Vercel**：https://vercel.com
2. **Railway**：https://railway.app
3. **Render**：https://render.com

### 操作步骤（以 Railway 为例）

**步骤 1：注册账号**
访问 https://railway.app 注册

**步骤 2：部署项目**
```bash
# 安装 Railway CLI
npm install -g @railway/cli

# 登录
railway login

# 初始化项目
cd /workspace/projects
railway init

# 部署
railway up
```

**步骤 3：配置环境变量**
在 Railway 控制台设置环境变量

**步骤 4：获取域名**
Railway 会自动分配一个域名

---

## 💡 我的建议

### 现在就想测试？→ 用方案一（内网穿透）
- 5 分钟就能在手机上测试
- 不需要任何成本
- 适合快速验证功能

### 打算长期运营？→ 用方案二（云服务器）
- 稳定可靠
- 适合正式发布
- 虽然需要一点成本，但可控

### 不想管理服务器？→ 用方案三（Serverless）
- 最简单
- 按使用付费
- 适合初期快速上线

---

## 🎯 下一步

**请告诉我**：
1. 你想现在就测试吗？（用方案一，我可以帮你启动内网穿透）
2. 还是打算长期运营？（用方案二，我可以提供更详细的云服务器配置指导）
3. 或者想用 Serverless？（用方案三）

我可以根据你的选择提供详细的操作指导！
