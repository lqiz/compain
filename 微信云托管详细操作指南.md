# 📱 微信云托管 - 详细操作步骤

## 🤔 你需要上传什么？

### 上传的是：整个项目代码

```
你的项目目录：
/workspace/projects/
├── server/              # 后端代码（必须）
├── src/                 # 前端代码
├── package.json         # 依赖配置（必须）
├── pnpm-lock.yaml       # 依赖锁文件（必须）
├── Dockerfile.tcb       # Docker配置（必须）
├── cloudbaserc.json     # 云开发配置（必须）
└── tcb.json            # 云托管配置（必须）
```

**重点**：
- ✅ 需要上传整个项目代码
- ✅ 包括后端代码、配置文件、依赖文件
- ❌ 不需要上传 node_modules（会自动安装）

---

## 🚀 详细操作步骤

### 准备工作

1. **下载微信开发者工具**
   - 访问：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   - 下载并安装（选择你的操作系统）

2. **登录微信小程序账号**
   - 打开微信开发者工具
   - 扫码登录
   - 需要一个小程序 AppID（如果没有，可以使用测试号）

---

### 步骤 1：开通微信云开发

1. **登录微信公众平台**
   - 浏览器访问：https://mp.weixin.qq.com
   - 扫码登录

2. **开通云开发**
   - 左侧菜单：开发 → 开发管理 → 云开发
   - 点击「开通」按钮
   - 勾选「同意」，点击「开通」

3. **选择基础版**
   - 选择「基础版」（免费）
   - 环境名称：输入 `prod`（或者自定义）
   - 点击「确定」

4. **获取环境ID**
   - 创建成功后，会显示环境ID
   - 格式类似：`your-env-id-xxx`
   - **复制这个环境ID！**（后面要用）

---

### 步骤 2：配置项目文件

#### 2.1 打开配置文件

在你的项目目录中找到以下文件：
- `cloudbaserc.json`
- `tcb.json`

#### 2.2 更新环境ID

**编辑 cloudbaserc.json**：
```json
{
  "envId": "你的环境ID",  // 把这里改成你的环境ID
  ...
}
```

**编辑 tcb.json**：
```json
{
  "envId": "你的环境ID",  // 把这里改成你的环境ID
  ...
}
```

#### 2.3 更新环境变量

**编辑 tcb.json**，在 `envVariables` 部分：

```json
"envVariables": [
  {
    "key": "COZE_BUCKET_ENDPOINT_URL",
    "value": "你的对象存储端点"  // 从 .env.local 复制
  },
  {
    "key": "COZE_BUCKET_NAME",
    "value": "你的存储桶名称"  // 从 .env.local 复制
  }
]
```

**如何获取对象存储配置？**
打开 `.env.local` 文件，复制这两个值。

---

### 步骤 3：在微信开发者工具中操作

#### 3.1 导入项目

1. **打开微信开发者工具**
2. **点击「+」号或「导入项目」**
3. **填写项目信息**：
   - 项目目录：选择你的项目目录（`/workspace/projects`）
   - AppID：填入你的小程序 AppID（或使用测试号）
   - 项目名称：可以随便填
   - 开发模式：小程序

4. **点击「导入」**

#### 3.2 打开云开发控制台

1. **在微信开发者工具顶部，点击「云开发」按钮**
2. **会自动打开云开发控制台**

#### 3.3 创建云托管服务

1. **在云开发控制台中，点击「云托管」标签**
2. **点击「新建服务」按钮**

3. **填写服务信息**：
   - 服务名称：`backend`（可以自定义）
   - 版本名称：`v1`（默认）
   - 点击「确定」

4. **选择部署方式**：
   - 选择「从本地代码创建」
   - 点击「确定」

#### 3.4 配置服务参数

1. **运行环境**：
   - 选择「Node.js 18」

2. **部署方式**：
   - 选择「Dockerfile」
   - Dockerfile路径：`Dockerfile.tcb`

3. **实例规格**：
   - 选择「0.5核 1GB」（基础版，免费）

4. **实例数量**：
   - 输入 `1`

5. **点击「创建」**

#### 3.5 等待部署

1. **点击「创建」后，会自动开始部署**
2. **等待 2-3 分钟**
3. **部署成功后，状态会显示「运行中」**

---

### 步骤 4：配置环境变量（重要！）

1. **在云托管页面，点击你的服务「backend」**
2. **点击「配置管理」标签**
3. **点击「添加环境变量」**

添加以下环境变量：

```
COZE_BUCKET_ENDPOINT_URL = 你的对象存储端点
COZE_BUCKET_NAME = 你的存储桶名称
NODE_ENV = production
PORT = 3000
```

4. **点击「保存」**

**注意**：这些值从 `.env.local` 文件中复制！

---

### 步骤 5：获取访问域名

1. **在云托管页面，点击「服务概览」**
2. **会看到一个「访问域名」**
3. **格式类似**：
   ```
   https://backend.your-env-id.tcb.qcloud.la
   ```

**复制这个域名！**

---

### 步骤 6：更新小程序配置

1. **打开 `.env.local` 文件**
2. **更新 `PROJECT_DOMAIN`**：
   ```env
   PROJECT_DOMAIN=https://backend.your-env-id.tcb.qcloud.la
   ```

3. **保存文件**

4. **重新编译小程序**
   - `coze dev` 会自动检测配置变化
   - 自动重新编译

---

### 步骤 7：测试部署

1. **在浏览器中访问 API**：
   ```
   https://backend.your-env-id.tcb.qcloud.la/api/video/list
   ```

2. **应该返回视频列表的 JSON 数据**

3. **在微信开发者工具中测试小程序**
   - 上传视频
   - 播放视频
   - 应该可以正常使用！

---

## 🎯 总结：你需要做什么

### 准备工作
1. ✅ 下载微信开发者工具
2. ✅ 登录微信小程序账号

### 配置工作
1. ✅ 开通微信云开发
2. ✅ 复制环境ID
3. ✅ 更新配置文件（cloudbaserc.json 和 tcb.json）

### 部署工作
1. ✅ 在微信开发者工具中导入项目
2. ✅ 创建云托管服务
3. ✅ 上传代码（微信开发者工具会自动上传）
4. ✅ 配置环境变量
5. ✅ 等待部署完成

### 测试工作
1. ✅ 获取访问域名
2. ✅ 更新小程序配置
3. ✅ 测试 API 和小程序

---

## 📞 遇到问题？

### Q1: 找不到「云开发」按钮？

**A**: 
1. 确保你已经开通了微信云开发
2. 在微信公众平台：开发 → 开发管理 → 云开发
3. 点击「开通」

### Q2: 上传失败怎么办？

**A**:
1. 检查 Dockerfile.tcb 是否存在
2. 检查配置文件是否正确
3. 查看部署日志，看看错误信息

### Q3: 部署成功但访问不了？

**A**:
1. 检查环境变量是否配置
2. 检查服务是否运行中
3. 等待 1-2 分钟，DNS 可能需要时间

### Q4: 如何重新部署？

**A**:
1. 云托管 → backend → 版本管理
2. 点击「新建版本」
3. 重新上传代码

---

**需要更多帮助？随时问我！**
